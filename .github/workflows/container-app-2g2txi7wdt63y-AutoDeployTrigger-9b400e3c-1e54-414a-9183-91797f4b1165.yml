name: Trigger auto deployment for container-app-2g2txi7wdt63y

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-2g2txi7wdt63y-AutoDeployTrigger-9b400e3c-1e54-414a-9183-91797f4b1165.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP2G2TXI7WDT63Y_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP2G2TXI7WDT63Y_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP2G2TXI7WDT63Y_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPP2G2TXI7WDT63Y_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP2G2TXI7WDT63Y_REGISTRY_PASSWORD }}
          containerAppName: container-app-2g2txi7wdt63y
          resourceGroup: aiprojectsforkidsdjangoa7e4
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


